@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@foreach (var item in Model)
{
    <div>
    @item.Name - @item.BloodGroup  <br />
        <a href="@Url.Action("Index", "Donors", new{ID = item.DonorID})">Details</a>
        &nbsp|&nbsp
        <a href="@Url.Action("Edit", "Donors", new {id= item.DonorID})">Edit</a>
        &nbsp|&nbsp
        <a href="@Url.Action("Delete", "Donors", new {id= item.DonorID})">Delete</a>
        </div>
}